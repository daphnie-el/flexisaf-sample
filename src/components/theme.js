import { lighten } from 'polished';

export const themeColor = "#17255A"
export const theme = {
    color: lighten(0.1, themeColor),
    secColor: '#f5f5f5',
    borderRadius: '5px', 
    border: '1px solid #ccc',
    textAlign: 'center'
}